# 设置服务器端口
server:
  port: 8080

# 开发环境数据源配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_easyshare
    username: root
    password: lizehan2001
  data:
    # redis配置
    redis:
      host: localhost
      port: 6379
    # mongodb配置
    mongodb:
      uri: mongodb+srv://lizehan2001:lizehan2001@cluster0.gwj8v.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      database: easyshare

# 生产环境配置
#spring:
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/db_easyshare
#    username: root
#    password: lizehan2001
#  data:
#    # redis配置
#    redis:
#      host: localhost
#      port: 6379
#      password: 123456
#    # mongodb配置
#    mongodb:
#      uri: mongodb+srv://lizehan2001:lizehan2001@cluster0.q2fzr.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
#      database: easyshare


# mybatis配置
mybatis:
  mapper-locations: classpath:dao/*.xml                      # 扫描dao .xml
  type-aliases-package: com.easyshare.pojo                   # 设置实体类别名
  configuration:
    cache-enabled: true                                      # 开启缓存
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl    # 打印sql日志

#阿里云OSS配置信息
aliyun:
  oss:
    endpoint: oss-cn-beijing.aliyuncs.com
    accessKeyId: LTAI5tDzYynhz19pbssL1HDz
    accessKeySecret: O7NvntFvWnfyqSYaBFGr5v4orMOWd9
    bucketName: lizehan

# socketio配置
socketio:
  host: 0.0.0.0
  port: 9999
  maxFramePayloadLength: 1048576   # 设置最大每帧处理数据的长度，防止他人利用大数据来攻击服务器
  maxHttpContentLength: 1048576    # 设置http交互最大内容长度
  bossCount: 1                     # socket连接数大小（如只监听一个端口boss线程组为1即可）
  workCount: 100
  allowCustomRequests: true
  upgradeTimeout: 1000000          # 协议升级超时时间（毫秒），默认10秒。HTTP握手升级为ws协议超时时间
  pingTimeout: 6000000             # Ping消息超时时间（毫秒），默认60秒，这个时间间隔内没有接收到心跳消息就会发送超时事件
  pingInterval: 25000              # Ping消息超时时间（毫秒），默认60秒，这个时间间隔内没有接收到心跳消息就会发送超时事件

# 日志设置
logging:
  level:
    com.easyshare.listener: trace

